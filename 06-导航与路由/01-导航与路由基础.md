# 导航与路由基础

> 掌握 Flutter 导航与路由系统，实现页面跳转和参数传递。

## 🧭 导航基础概念

### 1. 什么是导航
```dart
// 导航是移动应用的核心功能
// 用于在不同页面之间跳转
// Flutter 提供了强大的导航系统

// 导航类型：
// 1. 基本导航 - 简单的页面跳转
// 2. 命名路由 - 使用路由名称导航
// 3. 动态路由 - 运行时生成路由
// 4. 嵌套路由 - 多级导航结构
```

### 2. 路由栈概念
```dart
// 路由栈是一个后进先出(LIFO)的数据结构
// 新页面被推入(push)栈顶
// 返回时页面被弹出(pop)栈顶

// 示例：
// 栈状态变化：
// [] -> [HomePage] -> [DetailPage] -> [SettingsPage]
// 返回时：
// [SettingsPage] -> [DetailPage] -> [HomePage] -> []
```

## 🚀 基本导航

### 1. 页面跳转
```dart
// 基本页面跳转
class HomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('首页')),
      body: Center(
        child: ElevatedButton(
          onPressed: () {
            // 导航到新页面
            Navigator.push(
              context,
              MaterialPageRoute(
                builder: (context) => DetailPage(),
              ),
            );
          },
          child: Text('跳转到详情页'),
        ),
      ),
    );
  }
}

class DetailPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('详情页')),
      body: Center(
        child: ElevatedButton(
          onPressed: () {
            // 返回上一页
            Navigator.pop(context);
          },
          child: Text('返回'),
        ),
      ),
    );
  }
}
```

### 2. 页面传值
```dart
// 页面间传递数据
class HomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('首页')),
      body: Center(
        child: ElevatedButton(
          onPressed: () {
            // 传递数据到详情页
            Navigator.push(
              context,
              MaterialPageRoute(
                builder: (context) => DetailPage(
                  title: '传递的标题',
                  content: '传递的内容',
                ),
              ),
            );
          },
          child: Text('传递数据'),
        ),
      ),
    );
  }
}

class DetailPage extends StatelessWidget {
  final String title;
  final String content;
  
  DetailPage({required this.title, required this.content});
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text(title)),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            Text(content),
            ElevatedButton(
              onPressed: () {
                Navigator.pop(context);
              },
              child: Text('返回'),
            ),
          ],
        ),
      ),
    );
  }
}
```

### 3. 返回时传递数据
```dart
// 返回时传递数据
class HomePage extends StatefulWidget {
  @override
  _HomePageState createState() => _HomePageState();
}

class _HomePageState extends State<HomePage> {
  String _result = '';
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('首页')),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            Text('返回的结果: $_result'),
            ElevatedButton(
              onPressed: () async {
                // 导航并等待返回结果
                final result = await Navigator.push(
                  context,
                  MaterialPageRoute(
                    builder: (context) => InputPage(),
                  ),
                );
                
                // 处理返回结果
                if (result != null) {
                  setState(() {
                    _result = result;
                  });
                }
              },
              child: Text('输入数据'),
            ),
          ],
        ),
      ),
    );
  }
}

class InputPage extends StatelessWidget {
  final TextEditingController _controller = TextEditingController();
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('输入页')),
      body: Padding(
        padding: EdgeInsets.all(16),
        child: Column(
          children: [
            TextField(
              controller: _controller,
              decoration: InputDecoration(
                labelText: '输入内容',
                border: OutlineInputBorder(),
              ),
            ),
            SizedBox(height: 20),
            ElevatedButton(
              onPressed: () {
                // 返回数据
                Navigator.pop(context, _controller.text);
              },
              child: Text('返回'),
            ),
          ],
        ),
      ),
    );
  }
}
```

## 🏷️ 命名路由

### 1. 配置命名路由
```dart
// main.dart 中配置路由
void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Flutter Demo',
      // 初始路由
      initialRoute: '/',
      // 路由表
      routes: {
        '/': (context) => HomePage(),
        '/detail': (context) => DetailPage(),
        '/settings': (context) => SettingsPage(),
        '/profile': (context) => ProfilePage(),
      },
      // 未知路由处理
      onUnknownRoute: (settings) {
        return MaterialPageRoute(
          builder: (context) => NotFoundPage(),
        );
      },
    );
  }
}

// 使用命名路由导航
class HomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('首页')),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            ElevatedButton(
              onPressed: () {
                // 使用命名路由导航
                Navigator.pushNamed(context, '/detail');
              },
              child: Text('跳转到详情页'),
            ),
            ElevatedButton(
              onPressed: () {
                Navigator.pushNamed(context, '/settings');
              },
              child: Text('跳转到设置页'),
            ),
            ElevatedButton(
              onPressed: () {
                Navigator.pushNamed(context, '/profile');
              },
              child: Text('跳转到个人资料'),
            ),
          ],
        ),
      ),
    );
  }
}
```

### 2. 命名路由传参
```dart
// 使用 arguments 传递参数
class HomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('首页')),
      body: Center(
        child: ElevatedButton(
          onPressed: () {
            // 传递参数
            Navigator.pushNamed(
              context,
              '/detail',
              arguments: {
                'id': 123,
                'title': '参数标题',
                'content': '参数内容',
              },
            );
          },
          child: Text('传递参数'),
        ),
      ),
    );
  }
}

// 接收参数
class DetailPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // 获取路由参数
    final args = ModalRoute.of(context)!.settings.arguments as Map<String, dynamic>;
    final id = args['id'];
    final title = args['title'];
    final content = args['content'];
    
    return Scaffold(
      appBar: AppBar(title: Text(title)),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            Text('ID: $id'),
            Text('标题: $title'),
            Text('内容: $content'),
            ElevatedButton(
              onPressed: () {
                Navigator.pop(context);
              },
              child: Text('返回'),
            ),
          ],
        ),
      ),
    );
  }
}
```

### 3. onGenerateRoute 动态路由
```dart
// 使用 onGenerateRoute 处理动态路由
class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Flutter Demo',
      initialRoute: '/',
      routes: {
        '/': (context) => HomePage(),
        '/settings': (context) => SettingsPage(),
      },
      // 动态路由生成
      onGenerateRoute: (settings) {
        // 根据路由名称生成不同的路由
        if (settings.name == '/detail') {
          final args = settings.arguments as Map<String, dynamic>;
          return MaterialPageRoute(
            builder: (context) => DetailPage(
              id: args['id'],
              title: args['title'],
            ),
          );
        }
        
        if (settings.name == '/user') {
          final args = settings.arguments as Map<String, dynamic>;
          return MaterialPageRoute(
            builder: (context) => UserPage(userId: args['userId']),
          );
        }
        
        // 默认路由
        return MaterialPageRoute(
          builder: (context) => NotFoundPage(),
        );
      },
    );
  }
}

// 使用动态路由
class HomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('首页')),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            ElevatedButton(
              onPressed: () {
                Navigator.pushNamed(
                  context,
                  '/detail',
                  arguments: {
                    'id': 1,
                    'title': '动态路由',
                  },
                );
              },
              child: Text('动态路由'),
            ),
            ElevatedButton(
              onPressed: () {
                Navigator.pushNamed(
                  context,
                  '/user',
                  arguments: {
                    'userId': 123,
                  },
                );
              },
              child: Text('用户页面'),
            ),
          ],
        ),
      ),
    );
  }
}
```

## 🔄 高级导航

### 1. 页面替换
```dart
// pushReplacement - 替换当前页面
class LoginPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('登录页')),
      body: Center(
        child: ElevatedButton(
          onPressed: () {
            // 登录成功后，替换当前页面为首页
            Navigator.pushReplacement(
              context,
              MaterialPageRoute(
                builder: (context) => HomePage(),
              ),
            );
          },
          child: Text('登录'),
        ),
      ),
    );
  }
}

// pushReplacementNamed - 使用命名路由替换
class LoginPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('登录页')),
      body: Center(
        child: ElevatedButton(
          onPressed: () {
            // 使用命名路由替换
            Navigator.pushReplacementNamed(context, '/home');
          },
          child: Text('登录'),
        ),
      ),
    );
  }
}
```

### 2. 清空路由栈
```dart
// pushAndRemoveUntil - 清空路由栈
class LoginPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('登录页')),
      body: Center(
        child: ElevatedButton(
          onPressed: () {
            // 登录成功后，清空路由栈，只保留首页
            Navigator.pushAndRemoveUntil(
              context,
              MaterialPageRoute(
                builder: (context) => HomePage(),
              ),
              (route) => false, // 移除所有路由
            );
          },
          child: Text('登录'),
        ),
      ),
    );
  }
}

// pushNamedAndRemoveUntil - 使用命名路由清空栈
class LoginPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('登录页')),
      body: Center(
        child: ElevatedButton(
          onPressed: () {
            // 使用命名路由清空栈
            Navigator.pushNamedAndRemoveUntil(
              context,
              '/home',
              (route) => false,
            );
          },
          child: Text('登录'),
        ),
      ),
    );
  }
}
```

### 3. popUntil - 返回到指定页面
```dart
// 返回到指定页面
class SettingsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('设置页')),
      body: Center(
        child: ElevatedButton(
          onPressed: () {
            // 返回到首页
            Navigator.popUntil(
              context,
              ModalRoute.withName('/'),
            );
          },
          child: Text('返回首页'),
        ),
      ),
    );
  }
}

// 返回到上一个页面
class DetailPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('详情页')),
      body: Center(
        child: ElevatedButton(
          onPressed: () {
            // 返回上一个页面
            Navigator.pop(context);
          },
          child: Text('返回'),
        ),
      ),
    );
  }
}
```

## 🎯 嵌套路由

### 1. 底部导航嵌套路由
```dart
// 底部导航页面
class MainScreen extends StatefulWidget {
  @override
  _MainScreenState createState() => _MainScreenState();
}

class _MainScreenState extends State<MainScreen> {
  int _currentIndex = 0;
  
  // 每个标签页的导航键
  final List<GlobalKey<NavigatorState>> _navigatorKeys = [
    GlobalKey<NavigatorState>(),
    GlobalKey<NavigatorState>(),
    GlobalKey<NavigatorState>(),
  ];
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: IndexedStack(
        index: _currentIndex,
        children: [
          _buildNavigator(0, HomePage()),
          _buildNavigator(1, SearchPage()),
          _buildNavigator(2, ProfilePage()),
        ],
      ),
      bottomNavigationBar: BottomNavigationBar(
        currentIndex: _currentIndex,
        onTap: (index) {
          setState(() {
            _currentIndex = index;
          });
        },
        items: [
          BottomNavigationBarItem(icon: Icon(Icons.home), label: '首页'),
          BottomNavigationBarItem(icon: Icon(Icons.search), label: '搜索'),
          BottomNavigationBarItem(icon: Icon(Icons.person), label: '我的'),
        ],
      ),
    );
  }
  
  Widget _buildNavigator(int index, Widget child) {
    return Navigator(
      key: _navigatorKeys[index],
      onGenerateRoute: (settings) {
        return MaterialPageRoute(
          builder: (context) => child,
        );
      },
    );
  }
}

// 首页 - 支持内部导航
class HomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('首页')),
      body: Center(
        child: ElevatedButton(
          onPressed: () {
            // 在首页的导航器中导航
            Navigator.of(context).push(
              MaterialPageRoute(
                builder: (context) => DetailPage(),
              ),
            );
          },
          child: Text('查看详情'),
        ),
      ),
    );
  }
}
```

### 2. TabBar 嵌套路由
```dart
// TabBar 页面
class TabScreen extends StatefulWidget {
  @override
  _TabScreenState createState() => _TabScreenState();
}

class _TabScreenState extends State<TabScreen> with SingleTickerProviderStateMixin {
  late TabController _tabController;
  
  @override
  void initState() {
    super.initState();
    _tabController = TabController(length: 3, vsync: this);
  }
  
  @override
  void dispose() {
    _tabController.dispose();
    super.dispose();
  }
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('TabBar'),
        bottom: TabBar(
          controller: _tabController,
          tabs: [
            Tab(icon: Icon(Icons.home), text: '首页'),
            Tab(icon: Icon(Icons.search), text: '搜索'),
            Tab(icon: Icon(Icons.person), text: '我的'),
          ],
        ),
      ),
      body: TabBarView(
        controller: _tabController,
        children: [
          HomePage(),
          SearchPage(),
          ProfilePage(),
        ],
      ),
    );
  }
}
```

## 🛠️ 路由工具类

### 1. 路由管理器
```dart
// 路由管理器
class RouteManager {
  // 普通导航
  static Future<T?> push<T>(BuildContext context, Widget page) {
    return Navigator.push<T>(
      context,
      MaterialPageRoute(builder: (context) => page),
    );
  }
  
  // 命名路由导航
  static Future<T?> pushNamed<T>(BuildContext context, String routeName, {Object? arguments}) {
    return Navigator.pushNamed<T>(context, routeName, arguments: arguments);
  }
  
  // 替换当前页面
  static Future<T?> pushReplacement<T, TO>(BuildContext context, Widget page, {TO? result}) {
    return Navigator.pushReplacement<T, TO>(
      context,
      MaterialPageRoute(builder: (context) => page),
      result: result,
    );
  }
  
  // 清空栈并导航
  static Future<T?> pushAndRemoveUntil<T>(BuildContext context, Widget page) {
    return Navigator.pushAndRemoveUntil<T>(
      context,
      MaterialPageRoute(builder: (context) => page),
      (route) => false,
    );
  }
  
  // 返回
  static void pop<T>(BuildContext context, [T? result]) {
    Navigator.pop<T>(context, result);
  }
  
  // 返回到指定页面
  static void popUntil(BuildContext context, String routeName) {
    Navigator.popUntil(context, ModalRoute.withName(routeName));
  }
  
  // 可以返回
  static bool canPop(BuildContext context) {
    return Navigator.canPop(context);
  }
}

// 使用示例
class HomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('首页')),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            ElevatedButton(
              onPressed: () {
                RouteManager.push(context, DetailPage());
              },
              child: Text('普通导航'),
            ),
            ElevatedButton(
              onPressed: () {
                RouteManager.pushNamed(context, '/detail', arguments: {'id': 123});
              },
              child: Text('命名路由'),
            ),
            ElevatedButton(
              onPressed: () {
                RouteManager.pushReplacement(context, NewPage());
              },
              child: Text('替换页面'),
            ),
          ],
        ),
      ),
    );
  }
}
```

### 2. 路由拦截器
```dart
// 路由拦截器
class RouteInterceptor extends NavigatorObserver {
  @override
  void didPush(Route<dynamic> route, Route<dynamic>? previousRoute) {
    print('导航到: ${route.settings.name}');
    super.didPush(route, previousRoute);
  }
  
  @override
  void didPop(Route<dynamic> route, Route<dynamic>? previousRoute) {
    print('从 ${route.settings.name} 返回');
    super.didPop(route, previousRoute);
  }
  
  @override
  void didReplace({Route<dynamic>? newRoute, Route<dynamic>? oldRoute}) {
    print('替换路由: ${oldRoute?.settings.name} -> ${newRoute?.settings.name}');
    super.didReplace(newRoute: newRoute, oldRoute: oldRoute);
  }
  
  @override
  void didRemove(Route<dynamic> route, Route<dynamic>? previousRoute) {
    print('移除路由: ${route.settings.name}');
    super.didRemove(route, previousRoute);
  }
}

// 使用拦截器
class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Flutter Demo',
      navigatorObservers: [RouteInterceptor()],
      home: HomePage(),
    );
  }
}
```

## 🔧 常见问题解决

### 1. 路由冲突
```dart
// 问题：路由名称冲突
// 解决：使用完整的路由路径

// 不好的做法
routes: {
  '/': (context) => HomePage(),
  '/detail': (context) => DetailPage(),
  '/detail': (context) => AnotherDetailPage(), // 冲突
}

// 好的做法
routes: {
  '/': (context) => HomePage(),
  '/product/detail': (context) => ProductDetailPage(),
  '/user/detail': (context) => UserDetailPage(),
}
```

### 2. 参数传递问题
```dart
// 问题：参数类型不匹配
// 解决：使用类型安全的参数传递

// 不好的做法
Navigator.pushNamed(context, '/detail', arguments: {'id': '123'}); // 字符串

// 好的做法
Navigator.pushNamed(context, '/detail', arguments: DetailArguments(id: 123));

class DetailArguments {
  final int id;
  DetailArguments({required this.id});
}

// 接收参数
final args = ModalRoute.of(context)!.settings.arguments as DetailArguments;
final id = args.id; // 类型安全
```

### 3. 内存泄漏
```dart
// 问题：未正确释放资源
// 解决：在 dispose 中清理

class MyPage extends StatefulWidget {
  @override
  _MyPageState createState() => _MyPageState();
}

class _MyPageState extends State<MyPage> {
  late TextEditingController _controller;
  
  @override
  void initState() {
    super.initState();
    _controller = TextEditingController();
  }
  
  @override
  void dispose() {
    _controller.dispose(); // 清理资源
    super.dispose();
  }
  
  @override
  Widget build(BuildContext context) {
    return TextField(controller: _controller);
  }
}
```

## 📚 最佳实践

### 1. 路由组织
```dart
// 路由配置文件
class AppRoutes {
  static const String home = '/';
  static const String login = '/login';
  static const String detail = '/detail';
  static const String settings = '/settings';
  
  static Map<String, WidgetBuilder> getRoutes() {
    return {
      home: (context) => HomePage(),
      login: (context) => LoginPage(),
      detail: (context) => DetailPage(),
      settings: (context) => SettingsPage(),
    };
  }
  
  static Route<dynamic> onGenerateRoute(RouteSettings settings) {
    switch (settings.name) {
      case detail:
        final args = settings.arguments as DetailArguments;
        return MaterialPageRoute(
          builder: (context) => DetailPage(id: args.id),
        );
      default:
        return MaterialPageRoute(
          builder: (context) => NotFoundPage(),
        );
    }
  }
}

// 使用
class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Flutter Demo',
      initialRoute: AppRoutes.home,
      routes: AppRoutes.getRoutes(),
      onGenerateRoute: AppRoutes.onGenerateRoute,
    );
  }
}
```

### 2. 性能优化
```dart
// 1. 使用 const 构造函数
class MyPage extends StatelessWidget {
  const MyPage({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('我的页面')),
      body: const Center(child: Text('内容')),
    );
  }
}

// 2. 避免在路由中创建复杂对象
// 不好的做法
Navigator.push(
  context,
  MaterialPageRoute(
    builder: (context) => DetailPage(
      data: _expensiveDataProcessing(), // 在路由中执行耗时操作
    ),
  ),
);

// 好的做法
final data = _expensiveDataProcessing();
Navigator.push(
  context,
  MaterialPageRoute(
    builder: (context) => DetailPage(data: data),
  ),
);
```

## 🔗 相关链接

### 核心概念
- [[架构概览]] - Flutter 架构概览
- [[一切皆 Widget]] - 一切皆 Widget
- [[Dart 语言]] - Dart 语言

### 指南
- [[Widget 系统]] - Widget 系统详解
- [[状态管理]] - 状态管理方案
- [[UI 设计与动画]] - UI 设计与动画

### 实战项目
- [[电商应用]] - 电商应用实战
- [[社交应用]] - 社交应用开发

### 学习资源
- [[官方资源]] - 官方资源
- [[社区资源]] - 社区资源
- [[书籍推荐]] - 书籍推荐

---
*最后更新: 2026年5月23日*