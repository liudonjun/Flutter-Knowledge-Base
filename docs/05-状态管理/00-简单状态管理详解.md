# 简单状态管理详解

> 深入理解 Flutter 中的简单状态管理。

## 📖 简单状态管理基础

### 1. 什么是简单状态管理

```dart
// 简单状态管理概念
class SimpleStateManagementConcept {
  void explain() {
    print('''
    简单状态管理概念：
    
    // 1. 什么是简单状态管理
    // - 使用最简单的状态管理方式
    // - 适用于简单场景
    // - 无需额外依赖
    // - 易于理解和实现
    
    // 2. 简单状态管理的特点
    // - 简单易用：易于理解和实现
    // - 无需依赖：无需额外依赖
    // - 适用于简单场景
    // - 性能良好
    
    // 3. 简单状态管理的方式
    // - setState：局部状态
    // - ValueNotifier：简单状态
    // - StreamController：流状态
    // - ChangeNotifier：通知状态
    
    // 4. 简单状态管理的使用场景
    // - 表单输入
    // - 开关状态
    // - 计数器
    // - 简单交互
    ''');
  }
}
```

### 2. 简单状态管理实现

```dart
// 简单状态管理实现
class SimpleStateManagementImplementation {
  void explain() {
    print('''
    简单状态管理实现：
    
    // 1. setState
    class Counter extends StatefulWidget {
      @override
      _CounterState createState() => _CounterState();
    }
    
    class _CounterState extends State<Counter> {
      int _count = 0;
      
      @override
      Widget build(BuildContext context) {
        return Column(
          children: [
            Text('Count: $_count'),
            ElevatedButton(
              onPressed: () => setState(() => _count++),
              child: Text('Increment'),
            ),
          ],
        );
      }
    }
    
    // 2. ValueNotifier
    final counter = ValueNotifier<int>(0);
    
    ValueListenableBuilder<int>(
      valueListenable: counter,
      builder: (context, value, child) {
        return Text('Count: $value');
      },
    )
    
    // 3. StreamController
    final controller = StreamController<int>();
    
    StreamBuilder<int>(
      stream: controller.stream,
      builder: (context, snapshot) {
        return Text('Count: ${snapshot.data}');
      },
    )
    ''');
  }
}
```

## 🔗 相关链接

- [[setState详解]]
- [[ValueNotifier]]
- [[Stream]]
- [[ChangeNotifier]]

---

> 简单状态管理是入门状态管理的好方式，掌握它对于理解状态管理非常有帮助。