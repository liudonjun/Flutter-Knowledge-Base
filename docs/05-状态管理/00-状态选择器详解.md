# 状态选择器详解

> 深入理解 Flutter 中的状态选择器。

## 📖 状态选择器基础

### 1. 什么是状态选择器

```dart
// 状态选择器概念
class StateSelectorConcept {
  void explain() {
    print('''
    状态选择器概念：
    
    // 1. 什么是状态选择器
    // - 从状态中选择特定数据
    // - 减少重建范围
    // - 优化性能
    // - 提高代码可读性
    
    // 2. 状态选择器的特点
    // - 精确选择：只选择需要的数据
    // - 减少重建：只重建依赖的部分
    // - 性能优化：提高性能
    // - 代码简洁：提高可读性
    
    // 3. 状态选择器的方式
    // - Provider：Selector
    // - BLoC：BlocSelector
    // - Redux：mapStateToProps
    // - GetX：Obx
    
    // 4. 状态选择器的使用场景
    // - 复杂状态
    // - 性能优化
    // - 减少重建
    // - 精确监听
    ''');
  }
}
```

### 2. 状态选择器实现

```dart
// 状态选择器实现
class StateSelectorImplementation {
  void explain() {
    print('''
    状态选择器实现：
    
    // 1. Provider Selector
    Selector<Counter, int>(
      selector: (context, counter) => counter.count,
      builder: (context, count, child) {
        return Text('Count: $count');
      },
    )
    
    // 2. BLoC BlocSelector
    BlocSelector<CounterBloc, CounterState, int>(
      selector: (state) => state.count,
      builder: (context, count) {
        return Text('Count: $count');
      },
    )
    
    // 3. Redux mapStateToProps
    StoreConnector<AppState, ViewModel>(
      converter: (store) => ViewModel(
        count: store.state.count,
        name: store.state.name,
      ),
      builder: (context, viewModel) {
        return Text('${viewModel.name}: ${viewModel.count}');
      },
    )
    
    // 4. 状态选择器最佳实践
    // - 精确选择需要的数据
    // - 避免选择整个状态
    // - 使用 shouldRebuild 优化
    // - 考虑选择器性能
    ''');
  }
}
```

## 🔗 相关链接

- [[Selector]]
- [[Provider基础]]
- [[BLoC基础]]
- [[状态粒度]]

---

> 状态选择器是优化性能的重要工具，掌握它对于提高应用性能非常有帮助。