# 应用链接详解

> 深入理解 Flutter 中的应用链接。

## 📖 应用链接基础

### 1. 什么是应用链接

```dart
// 应用链接概念
class AppLinksConcept {
  void explain() {
    print('''
    应用链接概念：
    
    // 1. 什么是应用链接
    // - 从外部直接打开应用特定页面
    // - 通过 URL 传递参数
    // - 支持 Web 和移动端
    // - 实现应用间跳转
    
    // 2. 应用链接的特点
    // - 直接导航：跳过首页
    // - 参数传递：通过 URL 传递数据
    // - 跨平台：支持 Web、iOS、Android
    // - 用户体验：提高用户转化率
    
    // 3. 应用链接的使用场景
    // - 营销推广：直接打开活动页面
    // - 内容分享：分享应用内内容
    // - 通知推送：点击通知直接跳转
    // - 应用间协作：应用间数据传递
    ''');
  }
}
```

### 2. 应用链接配置

```dart
// 应用链接配置
class AppLinksConfiguration {
  void explain() {
    print('''
    应用链接配置：
    
    // 1. Android 配置
    // android/app/src/main/AndroidManifest.xml
    <intent-filter>
      <action android:name="android.intent.action.VIEW" />
      <category android:name="android.intent.category.DEFAULT" />
      <category android:name="android.intent.category.BROWSABLE" />
      <data
        android:scheme="https"
        android:host="example.com"
        android:pathPrefix="/product" />
    </intent-filter>
    
    // 2. iOS 配置
    // ios/Runner/Info.plist
    <key>CFBundleURLTypes</key>
    <array>
      <dict>
        <key>CFBundleURLSchemes</key>
        <array>
          <string>example</string>
        </array>
        <key>CFBundleURLName</key>
        <string>com.example.app</string>
      </dict>
    </array>
    
    // 3. Flutter 配置
    import 'package:uni_links/uni_links.dart';
    
    class AppLinkHandler {
      static Future<void> init() async {
        // 处理初始链接
        final initialLink = await getInitialLink();
        if (initialLink != null) {
          _handleLink(initialLink);
        }
        
        // 监听链接变化
        linkStream.listen((link) {
          if (link != null) {
            _handleLink(link);
          }
        });
      }
      
      static void _handleLink(String link) {
        final uri = Uri.parse(link);
        // 根据路径导航
        if (uri.path == '/product') {
          final id = uri.queryParameters['id'];
          Get.toNamed('/product/$id');
        }
      }
    }
    ''');
  }
}
```

## 🔧 应用链接使用

### 1. 链接处理

```dart
// 链接处理
class LinkHandling {
  void explain() {
    print('''
    链接处理：
    
    // 1. 解析链接
    final uri = Uri.parse('https://example.com/product/42?color=red');
    
    // 获取路径
    final path = uri.path;  // /product/42
    
    // 获取路径参数
    final segments = uri.pathSegments;  // ['product', '42']
    
    // 获取查询参数
    final color = uri.queryParameters['color'];  // red
    
    // 2. 路由匹配
    class LinkParser {
      static RouteSettings parse(String link) {
        final uri = Uri.parse(link);
        
        if (uri.path == '/product') {
          final id = uri.queryParameters['id'];
          return RouteSettings(
            name: '/product',
            arguments: {'id': id},
          );
        }
        
        return RouteSettings(name: '/');
      }
    }
    
    // 3. 参数验证
    class LinkValidator {
      static bool validate(Map<String, String> params) {
        if (params.containsKey('id')) {
          final id = int.tryParse(params['id']!);
          return id != null && id > 0;
        }
        return false;
      }
    }
    ''');
  }
}
```

### 2. 导航处理

```dart
// 导航处理
class NavigationHandling {
  void explain() {
    print('''
    导航处理：
    
    // 1. 使用 GetX 处理链接
    class LinkHandler {
      static void handle(String link) {
        final uri = Uri.parse(link);
        
        switch (uri.path) {
          case '/product':
            final id = uri.queryParameters['id'];
            Get.toNamed('/product/$id');
            break;
          case '/user':
            final username = uri.queryParameters['username'];
            Get.toNamed('/user/$username');
            break;
          default:
            Get.toNamed('/');
        }
      }
    }
    
    // 2. 使用 Navigator 处理链接
    void handleLink(String link) {
      final uri = Uri.parse(link);
      
      if (uri.path == '/product') {
        final id = uri.queryParameters['id'];
        Navigator.pushNamed(context, '/product', arguments: {'id': id});
      }
    }
    
    // 3. 错误处理
    void handleLinkWithError(String link) {
      try {
        final uri = Uri.parse(link);
        // 处理链接
      } catch (e) {
        // 处理错误
        print('Invalid link: $e');
      }
    }
    ''');
  }
}
```

## ⚠️ 注意事项

1. **应用链接需要平台特定配置**
2. **注意链接格式验证**
3. **处理无效链接**
4. **考虑安全性**

## 🔗 相关链接

- [[Navigator基础]]
- [[深度链接详解]]
- [[URL路由详解]]
- [[路由守卫详解]]

---

> 应用链接是提高用户转化率的重要手段，掌握它对于构建现代化应用非常重要。