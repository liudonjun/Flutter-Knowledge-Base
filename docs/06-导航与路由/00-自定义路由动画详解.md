# 自定义路由动画详解

> 深入理解 Flutter 中的自定义路由动画。

## 📖 自定义路由动画基础

### 1. 什么是自定义路由动画

```dart
// 自定义路由动画概念
class CustomRouteAnimationConcept {
  void explain() {
    print('''
    自定义路由动画概念：
    
    // 1. 什么是自定义路由动画
    // - 自定义页面切换时的动画效果
    // - 提供独特的视觉体验
    // - 增强应用个性化
    // - 遵循品牌设计规范
    
    // 2. 自定义路由动画的特点
    // - 独特性：提供独特的视觉效果
    // - 品牌化：符合品牌设计规范
    // - 用户体验：增强用户体验
    // - 灵活性：支持各种动画效果
    
    // 3. 自定义路由动画的使用场景
    // - 品牌应用：需要品牌化设计
    // - 特殊效果：需要特殊动画效果
    // - 用户体验：提升用户体验
    // - 差异化：与竞品形成差异
    ''');
  }
}
```

### 2. 自定义路由动画配置

```dart
// 自定义路由动画配置
class CustomRouteAnimationConfiguration {
  void explain() {
    print('''
    自定义路由动画配置：
    
    // 1. 使用 PageRouteBuilder
    Navigator.push(
      context,
      PageRouteBuilder(
        pageBuilder: (context, animation, secondaryAnimation) {
          return DetailsPage();
        },
        transitionsBuilder: (context, animation, secondaryAnimation, child) {
          return CustomTransition(animation: animation, child: child);
        },
      ),
    )
    
    // 2. 自定义 Transition
    class CustomTransition extends StatelessWidget {
      final Animation<double> animation;
      final Widget child;
      
      const CustomTransition({
        required this.animation,
        required this.child,
      });
      
      @override
      Widget build(BuildContext context) {
        return AnimatedBuilder(
          animation: animation,
          builder: (context, child) {
            return Transform.scale(
              scale: animation.value,
              child: Opacity(
                opacity: animation.value,
                child: child,
              ),
            );
          },
          child: child,
        );
      }
    }
    
    // 3. 使用第三方库
    import 'package:page_transition/page_transition.dart';
    
    Navigator.push(
      context,
      PageTransition(
        type: PageTransitionType.fade,
        child: DetailsPage(),
      ),
    )
    ''');
  }
}
```

## 🔧 自定义路由动画使用

### 1. 组合动画

```dart
// 组合动画
class CombinedAnimations {
  void explain() {
    print('''
    组合动画：
    
    // 1. 缩放 + 淡入淡出
    PageRouteBuilder(
      pageBuilder: (context, animation, secondaryAnimation) {
        return DetailsPage();
      },
      transitionsBuilder: (context, animation, secondaryAnimation, child) {
        return ScaleTransition(
          scale: animation,
          child: FadeTransition(
            opacity: animation,
            child: child,
          ),
        );
      },
    )
    
    // 2. 旋转 + 缩放
    PageRouteBuilder(
      pageBuilder: (context, animation, secondaryAnimation) {
        return DetailsPage();
      },
      transitionsBuilder: (context, animation, secondaryAnimation, child) {
        return RotationTransition(
          turns: animation,
          child: ScaleTransition(
            scale: animation,
            child: child,
          ),
        );
      },
    )
    
    // 3. 滑动 + 淡入淡出
    PageRouteBuilder(
      pageBuilder: (context, animation, secondaryAnimation) {
        return DetailsPage();
      },
      transitionsBuilder: (context, animation, secondaryAnimation, child) {
        return SlideTransition(
          position: Tween<Offset>(
            begin: Offset(1.0, 0.0),
            end: Offset.zero,
          ).animate(CurvedAnimation(
            parent: animation,
            curve: Curves.ease,
          )),
          child: FadeTransition(
            opacity: animation,
            child: child,
          ),
        );
      },
    )
    ''');
  }
}
```

### 2. 自定义曲线

```dart
// 自定义曲线
class CustomCurves {
  void explain() {
    print('''
    自定义曲线：
    
    // 1. 使用内置曲线
    Curves.ease
    Curves.easeIn
    Curves.easeOut
    Curves.easeInOut
    Curves.bounceIn
    Curves.bounceOut
    Curves.elasticIn
    Curves.elasticOut
    
    // 2. 自定义曲线
    final customCurve = Cubic(0.68, -0.55, 0.27, 1.55);
    
    PageRouteBuilder(
      pageBuilder: (context, animation, secondaryAnimation) {
        return DetailsPage();
      },
      transitionsBuilder: (context, animation, secondaryAnimation, child) {
        return SlideTransition(
          position: Tween<Offset>(
            begin: Offset(1.0, 0.0),
            end: Offset.zero,
          ).animate(CurvedAnimation(
            parent: animation,
            curve: customCurve,
          )),
          child: child,
        );
      },
    )
    
    // 3. 使用 TweenSequence
    final animation = TweenSequence<double>([
      TweenSequenceItem(
        tween: Tween<double>(begin: 0.0, end: 1.2),
        weight: 1.0,
      ),
      TweenSequenceItem(
        tween: Tween<double>(begin: 1.2, end: 1.0),
        weight: 1.0,
      ),
    ]).animate(controller);
    ''');
  }
}
```

## ⚠️ 注意事项

1. **自定义动画应该流畅自然**
2. **考虑性能优化**
3. **遵循品牌设计规范**
4. **测试不同设备**

## 🔗 相关链接

- [[Navigator基础]]
- [[页面跳转详解]]
- [[FadeTransition详解]]
- [[ScaleTransition详解]]

---

> 自定义路由动画是提升应用个性化的重要手段，掌握它对于创建独特的用户体验非常重要。