# 分页加载实现

> 掌握 Flutter 分页加载实现技巧。

## 📖 分页加载基础

### 1. 分页加载概念

```dart
// 分页加载概念
class PaginationConcepts {
  void explain() {
    print('''
    分页加载概念：
    
    // 1. 什么是分页加载
    // - 分批加载数据
    // - 按需加载
    // - 减少内存使用
    // - 提高性能
    
    // 2. 分页加载类型
    // - 传统分页：固定页码
    // - 滚动分页：滚动到底部加载
    // - 无限滚动：持续加载
    // - 预加载：提前加载下一页
    
    // 3. 分页加载的好处
    // - 减少内存使用
    // - 提高性能
    // - 改善用户体验
    // - 减少网络请求
    
    // 4. 分页加载的实现
    // - 使用 ScrollController
    // - 使用 PaginationController
    // - 使用 InfiniteScroll
    // - 使用 CustomScrollView
    ''');
  }
}
```

### 2. 分页加载性能分析

```dart
// 分页加载性能分析
class PaginationPerformanceAnalysis {
  void explain() {
    print('''
    分页加载性能分析：
    
    // 1. 使用 Performance Overlay
    MaterialApp(
      showPerformanceOverlay: true,
      home: MyHomePage(),
    )
    
    // 2. 使用 DevTools
    // - 打开 DevTools
    // - 切换到 Performance 面板
    // - 查看帧图表
    // - 分析分页加载性能
    
    // 3. 使用 Flutter Inspector
    // - 查看 Widget 树
    // - 分析分页加载结构
    // - 识别性能问题
    // - 优化分页加载
    
    // 4. 自定义性能分析
    class PaginationPerformanceTracker {
      final List<PaginationTiming> _timings = [];
      
      void startTracking() {
        SchedulerBinding.instance.addTimingsCallback((timings) {
          for (final timing in timings) {
            _timings.add(PaginationTiming(
              buildDuration: timing.buildDuration,
              rasterDuration: timing.rasterDuration,
            ));
          }
        });
      }
      
      void printReport() {
        print('分页加载性能报告:');
        for (final timing in _timings) {
          print('  build: ${timing.buildDuration.inMilliseconds}ms');
          print('  raster: ${timing.rasterDuration.inMilliseconds}ms');
        }
      }
    }
    ''');
  }
}
```

## 🔧 分页加载实现

### 1. 滚动分页加载

```dart
// 滚动分页加载
class ScrollPagination {
  void explain() {
    print('''
    滚动分页加载：
    
    // 1. 为什么使用滚动分页
    // - 用户体验好
    // - 自动加载
    // - 减少内存使用
    // - 提高性能
    
    // 2. 滚动分页示例
    class PaginatedListView extends StatefulWidget {
      @override
      _PaginatedListViewState createState() => _PaginatedListViewState();
    }
    
    class _PaginatedListViewState extends State<PaginatedListView> {
      final ScrollController _scrollController = ScrollController();
      final List<Item> _items = [];
      int _page = 0;
      bool _isLoading = false;
      
      @override
      void initState() {
        super.initState();
        _scrollController.addListener(_onScroll);
        _loadMore();
      }
      
      void _onScroll() {
        if (_scrollController.position.pixels ==
            _scrollController.position.maxScrollExtent) {
          _loadMore();
        }
      }
      
      Future<void> _loadMore() async {
        if (_isLoading) return;
        
        setState(() {
          _isLoading = true;
        });
        
        final newItems = await fetchItems(_page);
        setState(() {
          _items.addAll(newItems);
          _page++;
          _isLoading = false;
        });
      }
      
      @override
      Widget build(BuildContext context) {
        return ListView.builder(
          controller: _scrollController,
          itemCount: _items.length + (_isLoading ? 1 : 0),
          itemBuilder: (context, index) {
            if (index == _items.length) {
              return CircularProgressIndicator();
            }
            return ItemWidget(_items[index]);
          },
        );
      }
    }
    
    // 3. 滚动分页最佳实践
    // - 使用 ScrollController
    // - 使用加载指示器
    // - 使用错误处理
    // - 使用缓存
    
    // 4. 滚动分页注意事项
    // - 避免重复加载
    // - 使用加载指示器
    // - 使用错误处理
    // - 考虑性能影响
    ''');
  }
}
```

### 2. 预加载分页

```dart
// 预加载分页
class PreloadPagination {
  void explain() {
    print('''
    预加载分页：
    
    // 1. 为什么使用预加载
    // - 用户体验好
    // - 提前加载
    // - 减少等待时间
    // - 提高性能
    
    // 2. 预加载示例
    class PreloadListView extends StatefulWidget {
      @override
      _PreloadListViewState createState() => _PreloadListViewState();
    }
    
    class _PreloadListViewState extends State<PreloadListView> {
      final ScrollController _scrollController = ScrollController();
      final List<Item> _items = [];
      int _page = 0;
      bool _isLoading = false;
      
      @override
      void initState() {
        super.initState();
        _scrollController.addListener(_onScroll);
        _loadMore();
      }
      
      void _onScroll() {
        final position = _scrollController.position;
        final extentAfter = position.extentAfter;
        
        // 当剩余 200px 时预加载
        if (extentAfter < 200 && !_isLoading) {
          _loadMore();
        }
      }
      
      Future<void> _loadMore() async {
        if (_isLoading) return;
        
        setState(() {
          _isLoading = true;
        });
        
        final newItems = await fetchItems(_page);
        setState(() {
          _items.addAll(newItems);
          _page++;
          _isLoading = false;
        });
      }
      
      @override
      Widget build(BuildContext context) {
        return ListView.builder(
          controller: _scrollController,
          itemCount: _items.length + (_isLoading ? 1 : 0),
          itemBuilder: (context, index) {
            if (index == _items.length) {
              return CircularProgressIndicator();
            }
            return ItemWidget(_items[index]);
          },
        );
      }
    }
    
    // 3. 预加载最佳实践
    // - 使用 ScrollController
    // - 使用预加载阈值
    // - 使用加载指示器
    // - 使用缓存
    
    // 4. 预加载注意事项
    // - 预加载阈值要适当
    // - 避免重复加载
    // - 使用加载指示器
    // - 考虑性能影响
    ''');
  }
}
```

## 📊 分页加载分析

### 1. 使用 DevTools 分析分页加载

```dart
// DevTools 分页加载分析
class DevToolsPagination {
  void explain() {
    print('''
    DevTools 分页加载分析：
    
    // 1. 查看分页加载性能
    // - 打开 DevTools
    // - 切换到 Performance 面板
    // - 查看帧图表
    // - 分析分页加载性能
    
    // 2. 分析分页加载结构
    // - 使用 Flutter Inspector
    // - 查看 Widget 树
    // - 分析分页加载结构
    // - 识别性能问题
    
    // 3. 分页加载优化建议
    // - 使用滚动分页
    // - 使用预加载
    // - 使用缓存
    // - 使用错误处理
    
    // 4. 分页加载监控
    // - 监控分页加载性能
    // - 分析性能影响
    // - 优化分页加载
    // - 提高性能
    
    // 5. 分页加载最佳实践
    // - 保持分页加载简单
    // - 使用标准 Widget
    // - 避免复杂分页加载
    // - 考虑性能影响
    ''');
  }
}
```

## ⚠️ 注意事项

1. **分页加载类型**：选择合适的分页加载类型
2. **分页加载时机**：选择合适的分页加载时机
3. **分页加载性能**：监控分页加载性能，及时优化
4. **内存管理**：合理管理内存，避免内存泄漏
5. **测试设备**：在低端设备上测试分页加载性能

## 🔗 相关链接

- [[性能优化基础]]
- [[帧率监控与优化]]
- [[ListView优化]]
- [[懒加载]]

---

> 分页加载实现是提升 Flutter 应用性能的关键，通过优化分页加载可以显著提升用户体验。