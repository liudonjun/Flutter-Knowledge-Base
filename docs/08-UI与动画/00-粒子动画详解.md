# 粒子动画详解

> 深入理解 Flutter 中的粒子动画。

## 📖 粒子动画基础

### 1. 什么是粒子动画

```dart
// 粒子动画概念
class ParticleAnimationConcept {
  void explain() {
    print('''
    粒子动画概念：
    
    // 1. 什么是粒子动画
    // - 粒子效果的动画
    // - 模拟粒子运动
    // - 提供视觉效果
    // - 增强用户体验
    
    // 2. 粒子动画的特点
    // - 视觉效果：提供视觉效果
    // - 粒子模拟：模拟粒子运动
    // - 可定制：支持自定义粒子
    // - 性能优化：性能优化
    
    // 3. 粒子动画的类型
    // - 发射粒子：粒子发射效果
    // - 爆炸粒子：粒子爆炸效果
    // - 流动粒子：粒子流动效果
    // - 自定义粒子：自定义粒子效果
    
    // 4. 粒子动画的使用场景
    // - 特殊效果
    // - 游戏特效
    // - 交互反馈
    // - 装饰效果
    ''');
  }
}
```

### 2. 粒子动画实现

```dart
// 粒子动画实现
class ParticleAnimationImplementation {
  void explain() {
    print('''
    粒子动画实现：
    
    // 1. 粒子系统
    class Particle {
      Offset position;
      Offset velocity;
      Color color;
      double size;
      double life;
      
      Particle(this.position, this.velocity, this.color, this.size, this.life);
    }
    
    // 2. 粒子发射器
    class ParticleEmitter {
      List<Particle> particles = [];
      
      void emit(Offset position) {
        final particle = Particle(
          position,
          Offset(
            Random().nextDouble() * 2 - 1,
            Random().nextDouble() * 2 - 1,
          ),
          Colors.blue,
          Random().nextDouble() * 10 + 5,
          1.0,
        );
        particles.add(particle);
      }
      
      void update() {
        for (final particle in particles) {
          particle.position += particle.velocity;
          particle.life -= 0.01;
        }
        particles.removeWhere((p) => p.life <= 0);
      }
    }
    
    // 3. 绘制粒子
    class ParticlePainter extends CustomPainter {
      final List<Particle> particles;
      
      ParticlePainter(this.particles);
      
      @override
      void paint(Canvas canvas, Size size) {
        for (final particle in particles) {
          final paint = Paint()
            ..color = particle.color.withOpacity(particle.life);
          canvas.drawCircle(particle.position, particle.size, paint);
        }
      }
      
      @override
      bool shouldRepaint(covariant CustomPainter oldDelegate) => true;
    }
    ''');
  }
}
```

## 🔗 相关链接

- [[动画基础]]
- [[Canvas基础]]
- [[物理动画]]
- [[3D动画]]

---

> 粒子动画是创建特殊效果的重要方式，掌握它对于创建视觉特效非常有帮助。