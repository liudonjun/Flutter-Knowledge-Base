# 显式动画详解

> 深入理解 Flutter 中的显式动画。

## 📖 显式动画基础

### 1. 什么是显式动画

```dart
// 显式动画概念
class ExplicitAnimationConcept {
  void explain() {
    print('''
    显式动画概念：
    
    // 1. 什么是显式动画
    // - 手动控制动画
    // - 使用 AnimationController
    // - 精确控制动画
    // - 支持复杂动画
    
    // 2. 显式动画的特点
    // - 手动控制：手动控制动画
    // - 精确控制：精确控制参数
    // - 复杂动画：支持复杂动画
    // - 性能优化：性能优化
    
    // 3. 显式动画的类型
    // - AnimatedBuilder：动画构建器
    // - AnimatedWidget：动画 Widget
    // - Transition：过渡动画
    // - CustomPainter：自定义绘制
    
    // 4. 显式动画的使用场景
    // - 复杂动画
    // - 物理动画
    // - 粒子动画
    // - 自定义动画
    ''');
  }
}
```

### 2. 显式动画实现

```dart
// 显式动画实现
class ExplicitAnimationImplementation {
  void explain() {
    print('''
    显式动画实现：
    
    // 1. 使用 AnimationController
    class MyAnimation extends StatefulWidget {
      @override
      _MyAnimationState createState() => _MyAnimationState();
    }
    
    class _MyAnimationState extends State<MyAnimation>
        with SingleTickerProviderStateMixin {
      late AnimationController _controller;
      late Animation<double> _animation;
      
      @override
      void initState() {
        super.initState();
        _controller = AnimationController(
          duration: Duration(seconds: 1),
          vsync: this,
        );
        _animation = Tween<double>(
          begin: 0.0,
          end: 1.0,
        ).animate(_controller);
      }
      
      @override
      Widget build(BuildContext context) {
        return AnimatedBuilder(
          animation: _animation,
          builder: (context, child) {
            return Transform.scale(
              scale: _animation.value,
              child: Container(
                width: 100,
                height: 100,
                color: Colors.blue,
              ),
            );
          },
        );
      }
      
      @override
      void dispose() {
        _controller.dispose();
        super.dispose();
      }
    }
    
    // 2. 使用 Tween
    final animation = Tween<double>(
      begin: 0.0,
      end: 1.0,
    ).animate(_controller);
    
    // 3. 使用 CurvedAnimation
    final animation = CurvedAnimation(
      parent: _controller,
      curve: Curves.easeInOut,
    );
    
    // 4. 显式动画最佳实践
    // - 正确释放资源
    // - 使用适当曲线
    // - 考虑性能
    // - 测试动画效果
    ''');
  }
}
```

## 🔗 相关链接

- [[动画基础]]
- [[隐式动画]]
- [[动画曲线]]
- [[动画性能]]

---

> 显式动画是 Flutter 中精确控制动画的方式，掌握它对于创建复杂动画非常重要。