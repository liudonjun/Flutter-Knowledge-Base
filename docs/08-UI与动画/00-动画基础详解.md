# 动画基础详解

> 深入理解 Flutter 中的动画基础。

## 📖 动画基础

### 1. 什么是动画

```dart
// 动画概念
class AnimationBasicsConcept {
  void explain() {
    print('''
    动画概念：
    
    // 1. 什么是动画
    // - 视觉变化效果
    // - 随时间变化的属性
    // - 提供视觉反馈
    // - 增强用户体验
    
    // 2. 动画的特点
    // - 时间驱动：随时间变化
    // - 平滑过渡：平滑的视觉变化
    // - 可控性：可控制动画参数
    // - 性能优化：性能优化
    
    // 3. 动画的类型
    // - 隐式动画：自动动画
    // - 显式动画：手动控制
    // - 物理动画：物理模拟
    // - 粒子动画：粒子效果
    
    // 4. 动画的使用场景
    // - 页面转场
    // - 按钮反馈
    // - 加载动画
    // - 交互效果
    ''');
  }
}
```

### 2. 动画基础实现

```dart
// 动画基础实现
class AnimationBasicsImplementation {
  void explain() {
    print('''
    动画基础实现：
    
    // 1. 使用 AnimationController
    class MyAnimation extends StatefulWidget {
      @override
      _MyAnimationState createState() => _MyAnimationState();
    }
    
    class _MyAnimationState extends State<MyAnimation>
        with SingleTickerProviderStateMixin {
      late AnimationController _controller;
      
      @override
      void initState() {
        super.initState();
        _controller = AnimationController(
          duration: Duration(seconds: 1),
          vsync: this,
        );
      }
      
      @override
      Widget build(BuildContext context) {
        return AnimatedBuilder(
          animation: _controller,
          builder: (context, child) {
            return Transform.scale(
              scale: _controller.value,
              child: Container(
                width: 100,
                height: 100,
                color: Colors.blue,
              ),
            );
          },
        );
      }
      
      @override
      void dispose() {
        _controller.dispose();
        super.dispose();
      }
    }
    
    // 2. 使用 Tween
    final animation = Tween<double>(
      begin: 0.0,
      end: 1.0,
    ).animate(_controller);
    
    // 3. 使用 CurvedAnimation
    final animation = CurvedAnimation(
      parent: _controller,
      curve: Curves.easeInOut,
    );
    ''');
  }
}
```

## 🔗 相关链接

- [[隐式动画]]
- [[显式动画]]
- [[动画曲线]]
- [[动画性能]]

---

> 动画基础是 Flutter 动画系统的核心，掌握它对于创建流畅动画非常重要。