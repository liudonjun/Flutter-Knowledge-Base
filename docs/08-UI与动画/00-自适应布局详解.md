# 自适应布局详解

> 深入理解 Flutter 中的自适应布局。

## 📖 自适应布局基础

### 1. 什么是自适应布局

```dart
// 自适应布局概念
class AdaptiveLayoutConcept {
  void explain() {
    print('''
    自适应布局概念：
    
    // 1. 什么是自适应布局
    // - 根据屏幕尺寸调整布局
    // - 提供最佳的用户体验
    // - 支持多种设备
    // - 优化空间利用
    
    // 2. 自适应布局的特点
    // - 自适应：根据屏幕调整
    // - 最佳体验：提供最佳体验
    // - 多设备：支持多种设备
    // - 空间优化：优化空间利用
    
    // 3. 自适应布局的方式
    // - 断点设计：使用断点
    // - 弹性布局：使用弹性布局
    // - 网格布局：使用网格布局
    // - 响应式组件：使用响应式组件
    
    // 4. 自适应布局的使用场景
    // - 手机和平板
    // - 横竖屏切换
    // - 不同分辨率
    // - 折叠屏设备
    ''');
  }
}
```

### 2. 自适应布局实现

```dart
// 自适应布局实现
class AdaptiveLayoutImplementation {
  void explain() {
    print('''
    自适应布局实现：
    
    // 1. 使用断点
    class AdaptiveLayout extends StatelessWidget {
      @override
      Widget build(BuildContext context) {
        final screenWidth = MediaQuery.of(context).size.width;
        
        if (screenWidth < 600) {
          return PhoneLayout();
        } else if (screenWidth < 1200) {
          return TabletLayout();
        } else {
          return DesktopLayout();
        }
      }
    }
    
    // 2. 使用 GridView
    GridView.builder(
      gridDelegate: SliverGridDelegateWithFixedCrossAxisCount(
        crossAxisCount: MediaQuery.of(context).size.width > 600 ? 4 : 2,
      ),
      itemCount: items.length,
      itemBuilder: (context, index) {
        return ItemWidget(items[index]);
      },
    )
    
    // 3. 使用 Wrap
    Wrap(
      spacing: 8,
      runSpacing: 8,
      children: [
        Chip(label: Text('Tag 1')),
        Chip(label: Text('Tag 2')),
        Chip(label: Text('Tag 3')),
      ],
    )
    
    // 4. 自适应布局最佳实践
    // - 使用断点设计
    // - 测试不同设备
    // - 考虑用户体验
    // - 优化空间利用
    ''');
  }
}
```

## 🔗 相关链接

- [[响应式设计]]
- [[布局系统详解]]
- [[Material Design]]
- [[Cupertino设计]]

---

> 自适应布局是优化空间利用的重要方式，掌握它对于构建跨平台应用非常重要。