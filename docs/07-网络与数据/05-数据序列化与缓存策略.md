# 数据序列化与缓存策略

> 深入理解 Flutter 中的数据序列化、请求缓存、请求重试、数据分页等核心技术。

## 📖 数据序列化

### 1. JSON 序列化

```dart
// JSON 序列化基础
class JsonSerialization {
  /*
  JSON 序列化：
  
  1. 手动序列化
     - 手动编写 toJson/fromJson
     - 简单直接
     - 适合简单模型
  
  2. 代码生成
     - 使用 json_serializable
     - 自动生成代码
     - 类型安全
  
  3. 内置序列化
     - 使用 dart:convert
     - 基础功能
     - 适合简单场景
  */
  
  void explain() {
    print('''
    JSON 序列化：
    
    1. 手动序列化
       class User {
         final String name;
         final int age;
         
         User({required this.name, required this.age});
         
         factory User.fromJson(Map<String, dynamic> json) {
           return User(
             name: json['name'],
             age: json['age'],
           );
         }
         
         Map<String, dynamic> toJson() {
           return {
             'name': name,
             'age': age,
           };
         }
       }
    
    2. 代码生成 (json_serializable)
       @JsonSerializable()
       class User {
         final String name;
         final int age;
         
         User({required this.name, required this.age});
         
         factory User.fromJson(Map<String, dynamic> json) 
           => _$UserFromJson(json);
         
         Map<String, dynamic> toJson() => _$UserToJson(this);
       }
    
    3. 使用示例
       // 解析
       final json = jsonDecode(response.body);
       final user = User.fromJson(json);
       
       // 序列化
       final json = user.toJson();
       final body = jsonEncode(json);
    ''');
  }
}
```

### 2. 数据验证

```dart
// 数据验证
class DataValidation {
  /*
  数据验证：
  
  1. 类型验证
     - 检查数据类型
     - 类型转换
     - 空值处理
  
  2. 格式验证
     - 正则表达式
     - 格式检查
     - 范围检查
  
  3. 业务验证
     - 业务规则
     - 逻辑验证
     - 关联验证
  */
  
  void explain() {
    print('''
    数据验证：
    
    1. 类型验证
       factory User.fromJson(Map<String, dynamic> json) {
         if (json['name'] is! String) {
           throw FormatException('name must be a string');
         }
         if (json['age'] is! int) {
           throw FormatException('age must be an int');
         }
         return User(
           name: json['name'] as String,
           age: json['age'] as int,
         );
       }
    
    2. 空值处理
       factory User.fromJson(Map<String, dynamic> json) {
         return User(
           name: json['name'] as String? ?? '',
           age: json['age'] as int? ?? 0,
           email: json['email'] as String?,
         );
       }
    
    3. 格式验证
       bool isValidEmail(String email) {
         return RegExp(r'^[\\w-\\.]+@([\\w-]+\\.)+[\\w-]{2,4}$')
           .hasMatch(email);
       }
    ''');
  }
}
```

## 📖 请求缓存

### 1. 内存缓存

```dart
// 内存缓存
class MemoryCache {
  /*
  内存缓存：
  
  1. 简单缓存
     - Map 存储
     - 快速访问
     - 内存限制
  
  2. LRU 缓存
     - 最近最少使用
     - 自动淘汰
     - 容量控制
  
  3. 带过期时间
     - TTL 缓存
     - 自动过期
     - 定期清理
  */
  
  void explain() {
    print('''
    内存缓存：
    
    1. 简单缓存
       class SimpleCache {
         final Map<String, dynamic> _cache = {};
         
         void set(String key, dynamic value) {
           _cache[key] = value;
         }
         
         dynamic get(String key) {
           return _cache[key];
         }
         
         void remove(String key) {
           _cache.remove(key);
         }
       }
    
    2. LRU 缓存
       class LRUCache<K, V> {
         final int maxSize;
         final LinkedHashMap<K, V> _cache = LinkedHashMap();
         
         LRUCache(this.maxSize);
         
         V? get(K key) {
           if (!_cache.containsKey(key)) return null;
           
           // 移动到最新位置
           final value = _cache.remove(key)!;
           _cache[key] = value;
           return value;
         }
         
         void put(K key, V value) {
           if (_cache.containsKey(key)) {
             _cache.remove(key);
           } else if (_cache.length >= maxSize) {
             _cache.remove(_cache.keys.first);
           }
           _cache[key] = value;
         }
       }
    
    3. TTL 缓存
       class TTLCache<K, V> {
         final Duration ttl;
         final Map<K, _CacheEntry<V>> _cache = {};
         
         TTLCache({this.ttl = const Duration(minutes: 5)});
         
         V? get(K key) {
           final entry = _cache[key];
           if (entry == null) return null;
           
           if (DateTime.now().isAfter(entry.expiry)) {
             _cache.remove(key);
             return null;
           }
           
           return entry.value;
         }
         
         void put(K key, V value) {
           _cache[key] = _CacheEntry(
             value: value,
             expiry: DateTime.now().add(ttl),
           );
         }
       }
    ''');
  }
}
```

### 2. HTTP 缓存

```dart
// HTTP 缓存
class HttpCache {
  /*
  HTTP 缓存：
  
  1. 响应缓存
     - 缓存 HTTP 响应
     - 减少网络请求
     - 提高响应速度
  
  2. 缓存策略
     - Cache-First：优先使用缓存
     - Network-First：优先网络请求
     - Cache-Only：只使用缓存
     - Network-Only：只使用网络
  
  3. 缓存控制
     - 过期时间
     - 缓存大小
     - 清理策略
  */
  
  void explain() {
    print('''
    HTTP 缓存：
    
    1. 缓存拦截器
       class CacheInterceptor extends Interceptor {
         final Map<String, CacheEntry> _cache = {};
         
         @override
         void onRequest(RequestOptions options, RequestInterceptorHandler handler) {
           final key = _generateKey(options);
           final entry = _cache[key];
           
           if (entry != null && !entry.isExpired) {
             // 返回缓存的响应
             handler.resolve(entry.response);
             return;
           }
           
           handler.next(options);
         }
         
         @override
         void onResponse(Response response, ResponseInterceptorHandler handler) {
           final key = _generateKey(response.requestOptions);
           _cache[key] = CacheEntry(
             response: response,
             expiry: DateTime.now().add(Duration(hours: 1)),
           );
           handler.next(response);
         }
       }
    
    2. 缓存策略
       class CachePolicy {
         static const cacheFirst = 'cache_first';
         static const networkFirst = 'network_first';
         static const cacheOnly = 'cache_only';
         static const networkOnly = 'network_only';
       }
    
    3. 使用示例
       final dio = Dio();
       dio.interceptors.add(CacheInterceptor());
       
       // 第一次请求会调用网络
       final response1 = await dio.get('/api/data');
       
       // 第二次请求会使用缓存
       final response2 = await dio.get('/api/data');
    ''');
  }
}
```

## 📖 请求重试

### 1. 重试机制

```dart
// 请求重试
class RequestRetry {
  /*
  请求重试：
  
  1. 基本重试
     - 固定次数重试
     - 固定间隔
     - 简单实现
  
  2. 指数退避
     - 间隔递增
     - 避免拥塞
     - 更好恢复
  
  3. 条件重试
     - 特定错误重试
     - 网络错误重试
     - 服务器错误重试
  */
  
  void explain() {
    print('''
    请求重试：
    
    1. 基本重试
       Future<Response> retryRequest(
         Future<Response> Function() request,
         {int maxRetries = 3}
       ) async {
         for (int i = 0; i < maxRetries; i++) {
           try {
             return await request();
           } catch (e) {
             if (i == maxRetries - 1) rethrow;
             await Future.delayed(Duration(seconds: 1));
           }
         }
         throw Exception('Retry failed');
       }
    
    2. 指数退避
       Future<Response> retryWithBackoff(
         Future<Response> Function() request,
         {int maxRetries = 3, Duration initialDelay = const Duration(seconds: 1)}
       ) async {
         Duration delay = initialDelay;
         
         for (int i = 0; i < maxRetries; i++) {
           try {
             return await request();
           } catch (e) {
             if (i == maxRetries - 1) rethrow;
             
             await Future.delayed(delay);
             delay *= 2; // 指数退避
           }
         }
         throw Exception('Retry failed');
       }
    
    3. 重试拦截器
       class RetryInterceptor extends Interceptor {
         final int maxRetries;
         final Dio dio;
         
         RetryInterceptor({this.maxRetries = 3, required this.dio});
         
         @override
         void onError(DioError err, ErrorInterceptorHandler handler) async {
           if (_shouldRetry(err) && err.requestOptions.extra['retryCount'] < maxRetries) {
             err.requestOptions.extra['retryCount'] = 
               (err.requestOptions.extra['retryCount'] ?? 0) + 1;
             
             try {
               final response = await dio.fetch(err.requestOptions);
               handler.resolve(response);
             } catch (e) {
               handler.next(err);
             }
           } else {
             handler.next(err);
           }
         }
         
         bool _shouldRetry(DioError error) {
           return error.type == DioErrorType.connectionTimeout ||
                  error.type == DioErrorType.receiveTimeout ||
                  error.response?.statusCode == 500;
         }
       }
    ''');
  }
}
```

## 📖 数据分页

### 1. 分页加载

```dart
// 数据分页
class DataPagination {
  /*
  数据分页：
  
  1. 页码分页
     - 基于页码
     - 简单易懂
     - 适合传统后端
  
  2. 游标分页
     - 基于游标
     - 高效性能
     - 适合大数据量
  
  3. 无限滚动
     - 自动加载
     - 用户体验好
     - 内存管理
  */
  
  void explain() {
    print('''
    数据分页：
    
    1. 页码分页
       class PagePagination<T> {
         final int pageSize;
         int _currentPage = 0;
         bool _hasMore = true;
         final List<T> _items = [];
         
         PagePagination({this.pageSize = 20});
         
         Future<List<T>> loadPage(int page) async {
           final response = await dio.get(
             '/api/items',
             queryParameters: {
               'page': page,
               'limit': pageSize,
             },
           );
           return (response.data as List)
             .map((json) => Item.fromJson(json))
             .toList();
         }
         
         Future<bool> loadMore() async {
           if (!_hasMore) return false;
           
           final items = await loadPage(_currentPage + 1);
           _items.addAll(items);
           _currentPage++;
           _hasMore = items.length == pageSize;
           
           return items.isNotEmpty;
         }
       }
    
    2. 游标分页
       class CursorPagination<T> {
         final int pageSize;
         String? _cursor;
         bool _hasMore = true;
         final List<T> _items = [];
         
         Future<List<T>> loadPage(String? cursor) async {
           final response = await dio.get(
             '/api/items',
             queryParameters: {
               'cursor': cursor,
               'limit': pageSize,
             },
           );
           
           return PaginationResult.fromJson(response.data);
         }
         
         Future<bool> loadMore() async {
           if (!_hasMore) return false;
           
           final result = await loadPage(_cursor);
           _items.addAll(result.items);
           _cursor = result.nextCursor;
           _hasMore = result.nextCursor != null;
           
           return result.items.isNotEmpty;
         }
       }
    
    3. 无限滚动 Widget
       class InfiniteListView<T> extends StatefulWidget {
         final Future<List<T>> Function(String? cursor) fetchItems;
         final Widget Function(T item) itemBuilder;
         
         @override
         _InfiniteListViewState<T> createState() => _InfiniteListViewState<T>();
       }
       
       class _InfiniteListViewState<T> extends State<InfiniteListView<T>> {
         final List<T> _items = [];
         bool _isLoading = false;
         bool _hasMore = true;
         String? _cursor;
         
         @override
         void initState() {
           super.initState();
           _loadMore();
         }
         
         Future<void> _loadMore() async {
           if (_isLoading || !_hasMore) return;
           
           setState(() => _isLoading = true);
           
           final items = await widget.fetchItems(_cursor);
           setState(() {
             _items.addAll(items);
             _isLoading = false;
             _hasMore = items.isNotEmpty;
           });
         }
         
         @override
         Widget build(BuildContext context) {
           return ListView.builder(
             itemCount: _items.length + (_hasMore ? 1 : 0),
             itemBuilder: (context, index) {
               if (index == _items.length) {
                 _loadMore();
                 return CircularProgressIndicator();
               }
               return widget.itemBuilder(_items[index]);
             },
           );
         }
       }
    ''');
  }
}
```

## 📖 请求合并

### 1. 请求合并

```dart
// 请求合并
class RequestBatching {
  /*
  请求合并：
  
  1. 批量请求
     - 合并多个请求
     - 减少网络开销
     - 提高效率
  
  2. 防抖
     - 延迟发送
     - 合并连续请求
     - 减少请求次数
  
  3. 节流
     - 限制请求频率
     - 定时发送
     - 控制并发
  */
  
  void explain() {
    print('''
    请求合并：
    
    1. 批量请求
       class BatchRequest<T> {
         final List<Future<T>> _pending = [];
         Timer? _timer;
         
         void add(Future<T> request) {
           _pending.add(request);
           _scheduleBatch();
         }
         
         void _scheduleBatch() {
           _timer ??= Timer(Duration(milliseconds: 100), _executeBatch);
         }
         
         Future<void> _executeBatch() async {
           final requests = List.from(_pending);
           _pending.clear();
           _timer = null;
           
           await Future.wait(requests);
         }
       }
    
    2. 防抖
       class Debouncer {
         final Duration delay;
         Timer? _timer;
         
         Debouncer({this.delay = const Duration(milliseconds: 300)});
         
         void call(VoidCallback action) {
           _timer?.cancel();
           _timer = Timer(delay, action);
         }
         
         void cancel() {
           _timer?.cancel();
         }
       }
       
       // 使用
       final debouncer = Debouncer();
       onSearchChanged: (query) {
         debouncer(() => search(query));
       }
    
    3. 节流
       class Throttler {
         final Duration interval;
         Timer? _timer;
         bool _isThrottled = false;
         
         Throttler({this.interval = const Duration(seconds: 1)});
         
         void call(VoidCallback action) {
           if (_isThrottled) return;
           
           _isThrottled = true;
           action();
           
           _timer = Timer(interval, () {
             _isThrottled = false;
           });
         }
       }
    ''');
  }
}
```

## 📖 请求合并与数据压缩

### 1. 数据压缩

```dart
// 数据压缩
class DataCompression {
  /*
  数据压缩：
  
  1. 请求压缩
     - GZIP 压缩
     - 减少传输大小
     - 提高速度
  
  2. 响应压缩
     - 自动解压
     - 透明处理
     - 性能优化
  
  3. 图片压缩
     - 客户端压缩
     - 减少上传大小
     - 提高体验
  */
  
  void explain() {
    print('''
    数据压缩：
    
    1. GZIP 压缩
       class CompressionInterceptor extends Interceptor {
         @override
         void onRequest(RequestOptions options, RequestInterceptorHandler handler) {
           if (options.data != null && options.data is Map) {
             // 压缩请求体
             final json = jsonEncode(options.data);
             final compressed = gzip.encode(utf8.encode(json));
             
             options.data = compressed;
             options.headers['Content-Encoding'] = 'gzip';
           }
           handler.next(options);
         }
         
         @override
         void onResponse(Response response, ResponseInterceptorHandler handler) {
           if (response.headers.value('Content-Encoding') == 'gzip') {
             // 解压响应体
             final compressed = response.data as List<int>;
             final decompressed = gzip.decode(compressed);
             response.data = jsonDecode(utf8.decode(decompressed));
           }
           handler.next(response);
         }
       }
    
    2. 图片压缩
       Future<File> compressImage(File file) async {
         final result = await FlutterImageCompress.compressWithFile(
           file.absolute.path,
           quality: 85,
           minWidth: 1024,
           minHeight: 1024,
         );
         
         final compressedFile = File('${file.path}_compressed.jpg');
         await compressedFile.writeAsBytes(result!);
         
         return compressedFile;
       }
    
    3. 使用 Dio 配置
       final dio = Dio(BaseOptions(
         headers: {
           'Accept-Encoding': 'gzip, deflate',
         },
       ));
    ''');
  }
}
```

## 📖 离线缓存

### 1. 离线策略

```dart
// 离线缓存
class OfflineCache {
  /*
  离线缓存：
  
  1. 离线优先
     - 优先使用缓存
     - 后台同步
     - 离线可用
  
  2. 网络优先
     - 优先网络请求
     - 失败使用缓存
     - 保证数据新鲜
  
  3. 混合策略
     - 根据场景选择
     - 智能切换
     - 最佳体验
  */
  
  void explain() {
    print('''
    离线缓存：
    
    1. 离线优先
       class OfflineFirstRepository {
         final LocalDataSource local;
         final RemoteDataSource remote;
         
         Future<List<Item>> getItems() async {
           try {
             // 先尝试网络
             final items = await remote.getItems();
             // 缓存到本地
             await local.saveItems(items);
             return items;
           } catch (e) {
             // 网络失败，使用本地缓存
             return await local.getItems();
           }
         }
       }
    
    2. 同步策略
       class SyncManager {
         final Queue<SyncTask> _pendingTasks = Queue();
         
         Future<void> addTask(SyncTask task) async {
           _pendingTasks.add(task);
           await _processPendingTasks();
         }
         
         Future<void> _processPendingTasks() async {
           while (_pendingTasks.isNotEmpty) {
             final task = _pendingTasks.first;
             
             try {
               await task.execute();
               _pendingTasks.removeFirst();
             } catch (e) {
               // 保留待处理任务，稍后重试
               break;
             }
           }
         }
       }
    
    3. 冲突解决
       enum ConflictResolution {
         serverWins,  // 服务端优先
         clientWins,  // 客户端优先
         merge,       // 合并
       }
       
       Future<Item> resolveConflict(Item local, Item remote) async {
         if (remote.updatedAt.isAfter(local.updatedAt)) {
           return remote; // 更新的优先
         }
         return local;
       }
    ''');
  }
}
```

## 📖 总结

### 数据处理最佳实践

| 技术 | 使用场景 | 优势 |
|------|----------|------|
| **JSON 序列化** | 数据转换 | 类型安全，易维护 |
| **内存缓存** | 频繁访问数据 | 快速响应 |
| **HTTP 缓存** | 网络请求 | 减少请求 |
| **请求重试** | 网络不稳定 | 提高成功率 |
| **数据分页** | 大数据量 | 内存优化 |
| **请求合并** | 批量操作 | 减少请求 |
| **数据压缩** | 大数据传输 | 减少带宽 |
| **离线缓存** | 离线使用 | 用户体验 |

### 选择建议

1. **简单应用**：手动序列化 + 简单缓存
2. **中型应用**：json_serializable + HTTP 缓存
3. **大型应用**：完整缓存策略 + 离线支持

### 下一步学习

- **数据库详解**：本地数据持久化
- **实时数据**：WebSocket 和 SSE
- **数据同步**：多端数据同步

---

> 掌握数据序列化与缓存策略，构建高性能、可靠的 Flutter 应用。