# 文件读写详解

> 深入理解 Flutter 中的文件读写。

## 📖 文件读写基础

### 1. 什么是文件读写

```dart
// 文件读写概念
class FileReadWriteConcept {
  void explain() {
    print('''
    文件读写概念：
    
    // 1. 什么是文件读写
    // - 读取本地文件内容
    // - 写入数据到本地文件
    // - 支持多种文件格式
    // - 异步文件操作
    
    // 2. 文件读写的特点
    // - 异步操作：异步读写
    // - 多种格式：支持多种格式
    // - 流式操作：支持流式读写
    // - 错误处理：完善的错误处理
    
    // 3. 文件读写的方式
    // - 文本文件读写
    // - 二进制文件读写
    // - 流式读写
    // - 分块读写
    
    // 4. 文件读写的使用场景
    // - 配置文件
    // - 日志文件
    // - 缓存文件
    // - 数据文件
    ''');
  }
}
```

### 2. 文件读写实现

```dart
// 文件读写实现
class FileReadWriteImplementation {
  void explain() {
    print('''
    文件读写实现：
    
    // 1. 文本文件写入
    final file = File('${directory.path}/myfile.txt');
    await file.writeAsString('Hello World');
    
    // 2. 文本文件读取
    final content = await file.readAsString();
    print(content);
    
    // 3. 二进制文件写入
    final bytes = Uint8List.fromList([1, 2, 3, 4, 5]);
    await file.writeAsBytes(bytes);
    
    // 4. 二进制文件读取
    final bytes = await file.readAsBytes();
    print(bytes);
    
    // 5. 流式写入
    final sink = file.openWrite();
    sink.write('Line 1\n');
    sink.write('Line 2\n');
    await sink.close();
    
    // 6. 流式读取
    final stream = file.openRead();
    await for (final line in stream.transform(utf8.decoder).transform(LineSplitter())) {
      print(line);
    }
    
    // 7. 文件读写最佳实践
    // - 错误处理
    // - 编码处理
    // - 大文件处理
    // - 资源释放
    ''');
  }
}
```

## 🔗 相关链接

- [[文件管理]]
- [[临时文件]]
- [[文件上传]]
- [[文件下载]]

---

> 文件读写是本地数据存储的重要方式，掌握它对于管理本地数据非常重要。