# 请求缓存详解

> 深入理解 Flutter 中的请求缓存。

## 📖 请求缓存基础

### 1. 什么是请求缓存

```dart
// 请求缓存概念
class RequestCachingConcept {
  void explain() {
    print('''
    请求缓存概念：
    
    // 1. 什么是请求缓存
    // - 缓存网络请求结果
    // - 减少重复请求
    // - 提高响应速度
    // - 支持离线访问
    
    // 2. 请求缓存的特点
    // - 减少请求：减少重复请求
    // - 提高速度：提高响应速度
    // - 离线支持：支持离线访问
    // - 节省流量：节省网络流量
    
    // 3. 请求缓存的方式
    // - 内存缓存：存储在内存中
    // - 磁盘缓存：存储在磁盘上
    // - HTTP 缓存：HTTP 缓存头
    // - 自定义缓存：自定义缓存策略
    
    // 4. 请求缓存的使用场景
    // - API 数据
    // - 图片缓存
    // - 配置数据
    // - 用户数据
    ''');
  }
}
```

### 2. 请求缓存实现

```dart
// 请求缓存实现
class RequestCachingImplementation {
  void explain() {
    print('''
    请求缓存实现：
    
    // 1. 使用 Dio 缓存拦截器
    dio.interceptors.add(DioCacheManager(
      CacheConfig(baseUrl: 'https://api.example.com'),
    ).interceptor);
    
    // 2. 自定义缓存
    class CacheInterceptor extends Interceptor {
      final Map<String, Response> _cache = {};
      
      @override
      void onRequest(RequestOptions options, RequestInterceptorHandler handler) {
        final key = options.uri.toString();
        if (_cache.containsKey(key)) {
          handler.resolve(_cache[key]!);
        } else {
          handler.next(options);
        }
      }
      
      @override
      void onResponse(Response response, ResponseInterceptorHandler handler) {
        final key = response.requestOptions.uri.toString();
        _cache[key] = response;
        handler.next(response);
      }
    }
    
    // 3. HTTP 缓存
    final response = await dio.get(
      'https://api.example.com/data',
      options: Options(
        headers: {'Cache-Control': 'max-age=3600'},
      ),
    );
    
    // 4. 请求缓存最佳实践
    // - 缓存策略
    // - 过期处理
    // - 缓存清理
    // - 性能优化
    ''');
  }
}
```

## 🔗 相关链接

- [[Dio详解]]
- [[离线缓存]]
- [[请求重试]]
- [[数据缓存]]

---

> 请求缓存是优化网络请求的重要手段，掌握它对于提高应用性能非常有帮助。