# 请求重试详解

> 深入理解 Flutter 中的请求重试。

## 📖 请求重试基础

### 1. 什么是请求重试

```dart
// 请求重试概念
class RequestRetryConcept {
  void explain() {
    print('''
    请求重试概念：
    
    // 1. 什么是请求重试
    // - 请求失败后自动重试
    // - 提高请求成功率
    // - 处理网络波动
    // - 提升用户体验
    
    // 2. 请求重试的特点
    // - 自动重试：自动重试失败请求
    // - 可配置：重试次数可配置
    // - 智能重试：根据错误类型重试
    // - 指数退避：使用指数退避策略
    
    // 3. 请求重试的方式
    // - 固定间隔：固定时间间隔重试
    // - 指数退避：指数增加重试间隔
    // - 条件重试：根据条件重试
    // - 自定义重试：自定义重试策略
    
    // 4. 请求重试的使用场景
    // - 网络不稳定
    // - 服务器错误
    // - 超时错误
    // - 临时故障
    ''');
  }
}
```

### 2. 请求重试实现

```dart
// 请求重试实现
class RequestRetryImplementation {
  void explain() {
    print('''
    请求重试实现：
    
    // 1. 使用 Dio 重试拦截器
    dio.interceptors.add(
      RetryInterceptor(
        dio: dio,
        retries: 3,
        retryDelays: [
          Duration(seconds: 1),
          Duration(seconds: 2),
          Duration(seconds: 4),
        ],
      ),
    );
    
    // 2. 自定义重试拦截器
    class RetryInterceptor extends Interceptor {
      final int maxRetries;
      final Duration retryDelay;
      
      RetryInterceptor({this.maxRetries = 3, this.retryDelay = const Duration(seconds: 1)});
      
      @override
      void onError(DioError error, ErrorInterceptorHandler handler) async {
        final options = error.requestOptions;
        final retryCount = options.extra['retryCount'] ?? 0;
        
        if (retryCount < maxRetries) {
          options.extra['retryCount'] = retryCount + 1;
          await Future.delayed(retryDelay);
          final response = await dio.fetch(options);
          handler.resolve(response);
        } else {
          handler.next(error);
        }
      }
    }
    
    // 3. 指数退避重试
    Future<Response> retryWithBackoff(
      Future<Response> Function() request,
      int maxRetries,
    ) async {
      int retryCount = 0;
      while (retryCount < maxRetries) {
        try {
          return await request();
        } catch (e) {
          retryCount++;
          if (retryCount >= maxRetries) rethrow;
          await Future.delayed(Duration(seconds: pow(2, retryCount).toInt()));
        }
      }
      throw Exception('Max retries exceeded');
    }
    ''');
  }
}
```

## 🔗 相关链接

- [[Dio详解]]
- [[请求缓存]]
- [[错误处理]]
- [[网络超时]]

---

> 请求重试是提高网络请求可靠性的重要手段，掌握它对于构建稳定应用非常有帮助。