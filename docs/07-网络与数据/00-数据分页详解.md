# 数据分页详解

> 深入理解 Flutter 中的数据分页。

## 📖 数据分页基础

### 1. 什么是数据分页

```dart
// 数据分页概念
class DataPaginationConcept {
  void explain() {
    print('''
    数据分页概念：
    
    // 1. 什么是数据分页
    // - 分批加载数据
    // - 减少初始加载时间
    // - 优化内存使用
    // - 提升用户体验
    
    // 2. 数据分页的特点
    // - 按需加载：按需加载数据
    // - 减少内存：减少内存占用
    // - 提升性能：提升加载性能
    // - 用户体验：改善用户体验
    
    // 3. 数据分页的方式
    // - 页码分页：基于页码
    // - 游标分页：基于游标
    // - 偏移分页：基于偏移量
    // - 滚动分页：基于滚动位置
    
    // 4. 数据分页的使用场景
    // - 列表数据
    // - 搜索结果
    // - 新闻 feed
    // - 社交动态
    ''');
  }
}
```

### 2. 数据分页实现

```dart
// 数据分页实现
class DataPaginationImplementation {
  void explain() {
    print('''
    数据分页实现：
    
    // 1. 页码分页
    class PaginationController {
      int _currentPage = 1;
      final int _pageSize = 20;
      List<Item> _items = [];
      
      Future<void> loadMore() async {
        final newItems = await fetchItems(_currentPage, _pageSize);
        _items.addAll(newItems);
        _currentPage++;
      }
    }
    
    // 2. 游标分页
    class CursorPaginationController {
      String? _cursor;
      List<Item> _items = [];
      
      Future<void> loadMore() async {
        final result = await fetchItemsWithCursor(_cursor);
        _items.addAll(result.items);
        _cursor = result.nextCursor;
      }
    }
    
    // 3. 滚动分页
    final controller = ScrollController();
    
    controller.addListener(() {
      if (controller.position.pixels == controller.position.maxScrollExtent) {
        // 加载更多
        loadMore();
      }
    });
    
    // 4. 数据分页最佳实践
    // - 加载状态
    // - 错误处理
    // - 缓存策略
    // - 性能优化
    ''');
  }
}
```

## 🔗 相关链接

- [[GET请求]]
- [[列表懒加载]]
- [[分页加载实现]]
- [[数据缓存]]

---

> 数据分页是优化列表性能的重要手段，掌握它对于构建流畅的列表非常重要。